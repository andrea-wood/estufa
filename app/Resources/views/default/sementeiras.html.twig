{% extends 'base.html.twig' %}
{% block body %}
    <ol class="breadcrumb">
      <li><a href="{{ path('homepage') }}">Home</a></li>
      <li class="active">Sementeiras</li>
    </ol>
    
    <div class="content">
  		<h3>Sementeiras</h3>
  		<hr>
      <div class="jumbotron">
        <table class="table table-striped">
          <thead>
            <tr>
              <th>Nome da sementeira</th><th>Ultima atualização</th>
            </tr>
          </thead>
          <tbody>
            {% for sementeira in sementeiras %}
              <tr>
                <td><span class="glyphicon glyphicon-leaf" aria-hidden="true"></span> <a href="{{ path('sementeira', {'id': sementeira.id }) }}" >{{ sementeira.name }}</a></td>
                <td>{{ sementeira.updatedAt|date("d/m/Y - H:i") }}</td>
              </tr> 
            {% endfor %}
          </tbody>
        </table>
        <a href="{{ path('add_sementeira') }}" role="button" class="btn btn-primary add_sementeira_link"><span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Adicionar uma sementeira</a>
      </div>
    </div>
{% endblock %}

{% block stylesheets %}
<style>

</style>
{% endblock %}

